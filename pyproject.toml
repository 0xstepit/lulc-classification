[project]
name = "lulc-classification-with-unet"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "mdformat>=1.0.0",
    "pre-commit>=4.6.0",
]
